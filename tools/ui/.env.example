# These are Vite development variables only; llama-server does not read this file.
# Port 8033 is the SDK deployment convention and intentionally overrides Vite's 8080 fallback.
VITE_PUBLIC_APP_NAME='llama-ui'
VITE_DEBUG='true'
VITE_PUBLIC_SERVER_ORIGIN='http://localhost:8033'
