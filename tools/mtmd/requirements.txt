-r ../../requirements/requirements-convert_legacy_llama.txt
--extra-index-url https://download.pytorch.org/whl/cpu
pillow~=11.3.0

torch==2.11.0 # check_requirements: ignore "=="
# torchvision ships no s390x wheel upstream (unlike torch, which does); excluding it there
# rather than failing the whole install is deliberate, not a gating oversight.
torchvision==0.26.0; platform_machine != "s390x" # check_requirements: ignore "=="
